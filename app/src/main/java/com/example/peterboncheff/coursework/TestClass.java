package com.example.peterboncheff.coursework;

import android.content.Context;
import android.content.Intent;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;

@RunWith(JUnit4.class)
public class TestClass {

//    @Rule
//    public ActivityTestRule<MainActivity> activityActivityTestRule = new ActivityTestRule<MainActivity>(MainActivity.class);


//    @Before
//    public void init(){
//        activityActivityTestRule.getActivity()
//                .getSupportFragmentManager().beginTransaction();
//    }

    @Test
    public void TestAutoComplete(){
        //onView(withId(R.id.content_main_start_text)).check()
    }

    @Test
    public void testReverseUi(){
//        Context appContext = InstrumentationRegistry.getTargetContext();
//        onView(withId(R.id.et_textToReverse)).check(matches((isDisplayed()))));
//        onView(withId(R.id.et_textToReverse)).perform(clearText(), typeText("Mobile App Development"));
//        onView(withId(R.id.btn_reverse)).perform(click());
//        onView(withId(R.id.tv_reversedText)).check(matches(withText("asd")));
//        onView(withId(R.id.tv_reversedText)).check(matches(not(withText("hello"))));
    }
}
